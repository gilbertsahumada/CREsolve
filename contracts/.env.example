# ─── Local Deploy (Deploy.s.sol) ─────────────────────────────────
# Note: `KEYSTONE_FORWARDER` and `DIRECT_RESOLVER` are optional.
# `ERC8004_IDENTITY` + `ERC8004_REPUTATION` should point to deployed registries
# for a functional join flow. If both are `0x0`, `joinMarket` will revert.

# KeystoneForwarder address (CRE forwarder for on-chain report delivery)
# Sepolia: 0x15fC6ae953E024d975e77382eEeC56A9101f9F88
KEYSTONE_FORWARDER=0x0000000000000000000000000000000000000000

# Direct resolver address (authorize an EOA to resolve markets without CRE)
DIRECT_RESOLVER=0x0000000000000000000000000000000000000000

# ERC-8004 Identity Registry
# Sepolia: 0x8004A818BFB912233c491871b3d84c89A494BD9e
ERC8004_IDENTITY=0x0000000000000000000000000000000000000000

# ERC-8004 Reputation Registry
# Sepolia: 0x8004B663056A597Dffe9eCcC1965A193B7388713
ERC8004_REPUTATION=0x0000000000000000000000000000000000000000

# ─── Sepolia Deploy (DeploySepolia.s.sol) ────────────────────────
# Required for Sepolia deployment
# Also used by helper scripts:
# - yarn sepolia:sync
# - yarn sepolia:normalize
# - yarn sepolia:audit
# - yarn sepolia:deploy
# - yarn sepolia:verify

# Deployer private key (with 0x prefix)
DEPLOYER_KEY=0x...

# Sepolia RPC URL
SEPOLIA_RPC=https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY

# ─── Sepolia Fork Tests (CREsolverMarketFork.t.sol) ──────────────
# Required only when running:
# forge test --offline --match-contract CREsolverMarketForkTest -vv

# Worker wallet address that owns/is authorized for FORK_AGENT_ID in ERC-8004 Identity
FORK_WORKER_ADDRESS=0x0000000000000000000000000000000000000000

# ERC-8004 agent id associated to FORK_WORKER_ADDRESS
FORK_AGENT_ID=0
